# Port configuration (default 3000)
PORT=3000

# Optional: Facebook Graph API Access Token (App Token or User Token)
# If provided, the server can use it to query Graph API for higher rate limits
# FB_ACCESS_TOKEN=your_token_here
